All unit tests is in this folder.

Each class that is to be tested should have it's own class named same as the class that should be tested prepended with Test. The same goes for test methods, except these start with a lowercase letter (test) and then a descriptive name.